pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh '''
                    npm -v
                    node -v
                    ls -la
                '''
            }
        }
    }
}
